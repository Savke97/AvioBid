interface Flight {
    origin: string;
    destination: string;
    numberBC: number;
    numberEC: number;
    soldSeatsBC: number;
    travelDate: string;  // Date(travelDate)
}

export default Flight;